'use strict';

/**
 * carte-paiement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::carte-paiement.carte-paiement');
